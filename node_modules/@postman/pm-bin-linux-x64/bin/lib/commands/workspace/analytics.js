"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sendWorkspacePushEvents = sendWorkspacePushEvents;
const { v4: uuidv4 } = require('uuid'), { createClient, context: clickhouseContext } = require('../../framework/clickhouse');
async function sendWorkspacePushEvents({ workspaceId }) {
    const client = createClient(), hasAuth = await client.checkAuth(), eventSpan = uuidv4(), cliVersion = require('../../../package.json').version;
    if (!hasAuth) {
        return;
    }
    await clickhouseContext.insert(client, {
        eventSpan: eventSpan,
        executorType: 'workspace-push',
        workspaceId: workspaceId,
        cliVersion: cliVersion
    });
}
//# sourceMappingURL=analytics.js.map