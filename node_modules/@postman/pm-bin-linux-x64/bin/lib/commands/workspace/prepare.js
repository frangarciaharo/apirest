"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
const waterfall_1 = __importDefault(require("async/waterfall"));
const util_1 = __importDefault(require("../../postman-entities/util"));
const console_1 = __importDefault(require("../../framework/console"));
const workspace_utils_1 = require("./workspace-utils");
const safe_1 = __importDefault(require("colors/safe"));
module.exports = function (workspace) {
    workspace
        .command('prepare')
        .description('Prepare local collections and environments for push by validating and regenerating IDs if needed.')
        .option('--collections-dir <path>', 'Path to the collections directory', 'postman/collections')
        .option('--environments-dir <path>', 'Path to the environments directory', 'postman/environments')
        .action((command) => {
        let options, exitCode = 0, collectionStatuses = [], environmentStatuses = [];
        (0, waterfall_1.default)([
            (next) => {
                util_1.default.addApiKeyToOptions(command, {}, next);
            },
            (opts, next) => {
                options = opts;
                (0, workspace_utils_1.readPostmanConfig)()
                    .then(async (config) => {
                    const collectionFiles = await (0, workspace_utils_1.discoverEntities)('collections', process.cwd()), environmentFiles = await (0, workspace_utils_1.discoverEntities)('environments', process.cwd());
                    options.workspaceId = config.workspaceId;
                    options.collectionFiles = collectionFiles;
                    options.environmentFiles = environmentFiles;
                    console_1.default.log(safe_1.default.grey(`Found ${collectionFiles.length} collection(s)`));
                    console_1.default.log(safe_1.default.grey(`Found ${environmentFiles.length} environment(s)`));
                    next(null, options);
                })
                    .catch((err) => { next(err); });
            },
            (opts, next) => {
                options = opts;
                if ((options.collectionFiles?.length || 0) === 0 && (options.environmentFiles?.length || 0) === 0) {
                    return next(new Error('No collections or environments found to prepare'));
                }
                next(null, options);
            },
            (opts, next) => {
                options = opts;
                Promise.resolve()
                    .then(async () => {
                    collectionStatuses = await (0, workspace_utils_1.validateCollections)(options.collectionFiles || [], options);
                    environmentStatuses = await (0, workspace_utils_1.validateEnvironments)(options.environmentFiles || [], options);
                    await (0, workspace_utils_1.regenerateCollections)(collectionStatuses);
                    await (0, workspace_utils_1.regenerateEnvironments)(environmentStatuses);
                    (0, workspace_utils_1.printPrepareSummary)(collectionStatuses, environmentStatuses);
                })
                    .then(() => { return next(null); })
                    .catch((err) => { return next(err); });
            }
        ], (err) => {
            if (err) {
                console_1.default.error(safe_1.default.red(`\nError: ${err.message || err}`));
                logger.error(`Workspace prepare command failed: ${err.message || err}`);
                exitCode = 1;
            }
            process.exit(exitCode);
        });
    });
};
//# sourceMappingURL=prepare.js.map