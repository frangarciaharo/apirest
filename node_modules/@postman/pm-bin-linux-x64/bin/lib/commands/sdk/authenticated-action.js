"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.withAuthentication = withAuthentication;
const waterfall_1 = __importDefault(require("async/waterfall"));
const util_1 = __importDefault(require("../../postman-entities/util"));
const constants_1 = require("../../constants");
function withAuthentication(actionFn, config = {}) {
    const { requireAuth = true, errorMessage = constants_1.ERROR_MESSAGES.NO_AUTHORIZATION_DATA_WITH_REFERENCE } = config;
    return function (command) {
        let exitCode = 0;
        (0, waterfall_1.default)([
            (next) => {
                util_1.default.addApiKeyToOptions(command, {}, (err, options) => {
                    if (err) {
                        return next(err);
                    }
                    if (requireAuth && !options?.postmanApiKey && !options?.accessToken) {
                        return next(new Error(errorMessage));
                    }
                    next(null, options);
                });
            },
            actionFn
        ], (err) => {
            if (err) {
                process.stderr.write(`Error: ${err.message || err}\n`);
                exitCode = 1;
            }
            process.exit(exitCode);
        });
    };
}
module.exports = { withAuthentication };
//# sourceMappingURL=authenticated-action.js.map