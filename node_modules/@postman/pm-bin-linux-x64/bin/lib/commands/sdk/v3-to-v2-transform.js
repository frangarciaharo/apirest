"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformV3ToV2 = transformV3ToV2;
exports.isV3CollectionObject = isV3CollectionObject;
const v3_1 = require("@postman/runtime.models/v3");
const transforms_1 = require("@postman/runtime.models/transforms");
const V3 = __importStar(require("@postman/runtime.models/v3"));
function transformV3ToV2(v3Collection) {
    try {
        const cleanedCollection = { ...v3Collection };
        delete cleanedCollection.$schema;
        const validationResult = v3_1.Collection.validate(cleanedCollection);
        if (validationResult.issues) {
            console.error('V3 collection validation issues:', JSON.stringify(validationResult.issues, null, 2));
            throw Object.assign(new Error('Invalid input for model'), { issues: validationResult.issues });
        }
        const parsedV3 = validationResult.value;
        const isCompatible = V3.isV2Compatible(parsedV3);
        if (!isCompatible) {
            console.warn('Warning: V3 collection may not be fully compatible with V2 format. ' +
                'Some features may be lost or modified during transformation.');
        }
        const v2Collection = (0, transforms_1.transform)(v3_1.Collection, transforms_1.FormatVersion.V2, parsedV3);
        return v2Collection;
    }
    catch (error) {
        const errorMessage = error instanceof Error ? error.message : String(error);
        const errorStack = error instanceof Error ? error.stack : undefined;
        console.error('Failed to transform V3 collection to V2 format:', {
            error: errorMessage,
            stack: errorStack
        });
        if (error.issues) {
            console.error('Validation issues:', JSON.stringify(error.issues, null, 2));
        }
        throw new Error(`Failed to transform V3 collection to V2: ${errorMessage}`);
    }
}
function isV3CollectionObject(collection) {
    if (!collection || typeof collection !== 'object') {
        return false;
    }
    return (collection.$kind === 'collection' &&
        (collection.$schema?.includes('/v3.') || collection.$schema?.includes('/v3/')));
}
//# sourceMappingURL=v3-to-v2-transform.js.map