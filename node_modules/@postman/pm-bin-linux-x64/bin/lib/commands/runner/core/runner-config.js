const runnerConfig = {
    health: {
        restartIfUnhealthy: true,
        noRunEventTimeout: 3605000
    },
    http: {
        port: 1337,
        maxBodySize: 50 * 1024 * 1024,
        keepAliveTimeout: 35000
    },
    timeout: 3600000,
    maxRunAttempts: 1,
    healthyConsecutiveCount: 1,
    useWhatWGUrlParser: true,
    limits: {
        maxIterationCount: 100000000
    },
    lazyDelay: 5000,
    agentErrorDelay: 1000,
    authenticationRetryLimit: 3,
    pingInterval: parseInt(process.env.PING_INTERVAL_MS, 10) || 5000,
    logger: {
        enabled: true,
        level: process.env.LOG_LEVEL || 'info',
        service: 'postman-runner',
        prettyFormat: process.env.LOG_FORMAT_PRETTY === 'true' || false
    }
};
module.exports.get = () => {
    return runnerConfig;
};
//# sourceMappingURL=runner-config.js.map