const RunnerLogger = require('./index');
const runnerConfig = require('../core/runner-config');
let loggerInstance = null;
function getLogger(config = null) {
    if (!loggerInstance) {
        const defaultConfig = runnerConfig.get().logger || {};
        loggerInstance = new RunnerLogger({
            ...defaultConfig,
            ...config
        });
    }
    return loggerInstance;
}
function resetLogger() {
    loggerInstance = null;
}
module.exports = {
    getLogger,
    resetLogger
};
//# sourceMappingURL=logger-factory.js.map