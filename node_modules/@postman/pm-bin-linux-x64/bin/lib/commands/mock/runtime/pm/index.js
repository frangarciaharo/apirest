const { initialize: initMock, pmMockAPI } = require('./mock');
const { initialize: initEnvironment, pmEnvironmentAPI } = require('./environment');
const { initialize: initGlobals, pmGlobalsAPI } = require('./globals');
function installPmGlobal({ examples = [], requests = [], environment = [], globals = [] } = {}) {
    initMock(examples, requests);
    initEnvironment(environment);
    initGlobals(globals);
    if (!global.pm) {
        global.pm = {};
    }
    global.pm.mock = pmMockAPI;
    global.pm.environment = pmEnvironmentAPI;
    global.pm.globals = pmGlobalsAPI;
}
module.exports = { installPmGlobal };
//# sourceMappingURL=index.js.map