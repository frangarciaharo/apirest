const { matchRequest } = require('../matching-algorithm');
describe('Request Matching Algorithm', () => {
    describe('Exact path matching', () => {
        it('should match exact paths', () => {
            const incoming = { method: 'GET', url: '/users' };
            const saved = { method: 'GET', path: '/users', url: 'http://localhost:3000/users' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should match exact paths with query strings', () => {
            const incoming = { method: 'GET', url: '/users?page=1&limit=10' };
            const saved = { method: 'GET', path: '/users', url: 'http://localhost:3000/users' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should NOT match different paths', () => {
            const incoming = { method: 'GET', url: '/users' };
            const saved = { method: 'GET', path: '/posts', url: 'http://localhost:3000/posts' };
            expect(matchRequest(incoming, saved)).toBe(false);
        });
    });
    describe('Path variable matching (:id style)', () => {
        it('should match single path variable', () => {
            const incoming = { method: 'GET', url: '/users/123' };
            const saved = { method: 'GET', path: '/users/:id', url: 'http://localhost:3000/users/:id' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should match multiple path variables', () => {
            const incoming = { method: 'GET', url: '/users/123/posts/456' };
            const saved = { method: 'GET', path: '/users/:userId/posts/:postId', url: 'http://localhost:3000/users/:userId/posts/:postId' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should match path variable with query string', () => {
            const incoming = { method: 'GET', url: '/users/123?include=profile' };
            const saved = { method: 'GET', path: '/users/:id', url: 'http://localhost:3000/users/:id' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should match path variable in the middle', () => {
            const incoming = { method: 'GET', url: '/check/123/palindrome' };
            const saved = { method: 'GET', path: '/check/:id/palindrome', url: 'http://localhost:4500/check/:id/palindrome' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should NOT match if segment count differs', () => {
            const incoming = { method: 'GET', url: '/users/123/extra' };
            const saved = { method: 'GET', path: '/users/:id', url: 'http://localhost:3000/users/:id' };
            expect(matchRequest(incoming, saved)).toBe(false);
        });
        it('should NOT match if static segments differ', () => {
            const incoming = { method: 'GET', url: '/posts/123' };
            const saved = { method: 'GET', path: '/users/:id', url: 'http://localhost:3000/users/:id' };
            expect(matchRequest(incoming, saved)).toBe(false);
        });
    });
    describe('Postman variable matching ({{id}} style)', () => {
        it('should match single unresolved Postman variable', () => {
            const incoming = { method: 'GET', url: '/users/123' };
            const saved = { method: 'GET', path: '/users/{{id}}', url: 'http://localhost:3000/users/{{id}}' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should match multiple unresolved Postman variables', () => {
            const incoming = { method: 'GET', url: '/users/123/posts/456' };
            const saved = { method: 'GET', path: '/users/{{userId}}/posts/{{postId}}', url: 'http://localhost:3000/users/{{userId}}/posts/{{postId}}' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should match Postman variable in the middle', () => {
            const incoming = { method: 'GET', url: '/check/123/palindrome' };
            const saved = { method: 'GET', path: '/check/{{id}}/palindrome', url: 'http://localhost:4500/check/{{id}}/palindrome' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should match Postman variable with query string', () => {
            const incoming = { method: 'GET', url: '/check/123/palindrome?input=racecar' };
            const saved = { method: 'GET', path: '/check/{{id}}/palindrome', url: 'http://localhost:4500/check/{{id}}/palindrome' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
    });
    describe('Method matching', () => {
        it('should match same HTTP methods', () => {
            const incoming = { method: 'POST', url: '/users' };
            const saved = { method: 'POST', path: '/users', url: 'http://localhost:3000/users' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should match methods case-insensitively', () => {
            const incoming = { method: 'get', url: '/users' };
            const saved = { method: 'GET', path: '/users', url: 'http://localhost:3000/users' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should NOT match different HTTP methods', () => {
            const incoming = { method: 'GET', url: '/users' };
            const saved = { method: 'POST', path: '/users', url: 'http://localhost:3000/users' };
            expect(matchRequest(incoming, saved)).toBe(false);
        });
    });
    describe('Complex scenarios', () => {
        it('should match complex path with multiple variables and query params', () => {
            const incoming = { method: 'GET', url: '/api/v1/users/123/posts/456/comments?sort=date&order=desc' };
            const saved = { method: 'GET', path: '/api/v1/users/:userId/posts/:postId/comments', url: 'http://localhost:3000/api/v1/users/:userId/posts/:postId/comments' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should handle trailing slashes correctly', () => {
            const incoming = { method: 'GET', url: '/users/' };
            const saved = { method: 'GET', path: '/users', url: 'http://localhost:3000/users' };
            expect(matchRequest(incoming, saved)).toBe(false);
        });
        it('should handle root path', () => {
            const incoming = { method: 'GET', url: '/' };
            const saved = { method: 'GET', path: '/', url: 'http://localhost:3000/' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
        it('should handle empty segments gracefully', () => {
            const incoming = { method: 'GET', url: '/users//123' };
            const saved = { method: 'GET', path: '/users/:id', url: 'http://localhost:3000/users/:id' };
            expect(matchRequest(incoming, saved)).toBe(true);
        });
    });
    describe('Edge cases', () => {
        it('should handle missing method', () => {
            const incoming = { url: '/users' };
            const saved = { method: 'GET', path: '/users', url: 'http://localhost:3000/users' };
            expect(matchRequest(incoming, saved)).toBe(false);
        });
        it('should handle missing URL', () => {
            const incoming = { method: 'GET' };
            const saved = { method: 'GET', path: '/users', url: 'http://localhost:3000/users' };
            expect(matchRequest(incoming, saved)).toBe(false);
        });
        it('should handle null/undefined requests', () => {
            expect(matchRequest(null, { method: 'GET', path: '/users' })).toBe(false);
            expect(matchRequest({ method: 'GET', url: '/users' }, null)).toBe(false);
        });
    });
});
//# sourceMappingURL=matching-algorithm.test.js.map