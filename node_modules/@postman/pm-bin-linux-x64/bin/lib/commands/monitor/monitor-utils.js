const _ = require('lodash');
function getRunStatus(run) {
    if (_.get(run, 'stats.errorCount', 0) > 0) {
        return 'error';
    }
    else if (_.get(run, 'stats.failedTestCount', 0) > 0) {
        return 'failed';
    }
    const runStatusString = _.get(run, 'status', '');
    if (runStatusString && typeof runStatusString === 'string') {
        const statusChecks = ['running', 'aborted', 'success', 'error', 'failed'];
        const lowerStatusString = runStatusString.toLowerCase();
        const foundStatus = statusChecks.find((status) => {
            return lowerStatusString.includes(status);
        });
        if (foundStatus) {
            return foundStatus;
        }
    }
    return 'unknown';
}
module.exports = {
    getRunStatus
};
//# sourceMappingURL=monitor-utils.js.map