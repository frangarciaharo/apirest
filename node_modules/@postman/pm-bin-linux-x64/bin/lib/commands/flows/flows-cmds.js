"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = flowsCommand;
const flows_list_1 = __importDefault(require("./flows-list"));
const handler_1 = require("./cmd/handler");
const flows_trigger_1 = __importDefault(require("./flows-trigger"));
const flows_deploy_1 = __importDefault(require("./flows-deploy"));
const flows_update_1 = __importDefault(require("./flows-update"));
const flows_list_runs_1 = __importStar(require("./flows-list-runs"));
const flows_analyze_runs_1 = __importDefault(require("./flows-analyze-runs"));
const flows_run_1 = require("./run/flows-run");
const input_functions_1 = require("./run/input-functions");
function flowsCommand(program) {
    const flows = program
        .command('flows')
        .name('flows')
        .alias('fl')
        .addHelpCommand(false)
        .description('Manage and interact with flows.');
    (0, handler_1.addCommonOptions)(flows
        .command('list')
        .description('List all flows')
        .usage('[options]')
        .requiredOption('-w, --workspace <workspaceId>', 'Specify the workspace ID to list flows from')
        .option('-p, --paginate', 'Paginates the list of flows')
        .option('-f, --filter <pattern>', 'Filter flows by name. Pattern may be a regex, or a name prefix.')
        .option('-s, --sort <criteria>', 'Sort flows by criteria: "name" or "updated" (default: updated)', (value) => {
        const validValues = ['name', 'updated'];
        if (!validValues.includes(value)) {
            throw new Error(`Invalid sort option ${value}. Must be one of: ${validValues.join(', ')}`);
        }
        return value;
    }, 'updated')
        .addHelpText('after', '\nExamples:\n' +
        '  postman flows list --workspace 12345-67890-abcdef\n' +
        '  postman flows list --workspace 12345-67890-abcdef --filter "My"\n' +
        '  postman flows list --workspace 12345-67890-abcdef --filter "^Test.*" --paginate\n' +
        '  postman flows list --workspace 12345-67890-abcdef --sort name\n' +
        '  postman flows list --workspace 12345-67890-abcdef --sort updated --filter "API"\n')
        .action((options) => {
        return (0, handler_1.validateAndRunCommand)(options, flows_list_1.default);
    }));
    (0, handler_1.addCommonOptions)(flows
        .command('trigger <flowId>')
        .description('Trigger a deployed flow')
        .usage('<flowId> [options]')
        .option('-i, --input <key=value>', 'Set key=value pairs in the trigger payload (e.g. --input foo=bar --input baz=qux)', input_functions_1.castInput, [])
        .option('-f, --input-file <path.json>', 'Set payload values from JSON files (e.g. --input-file ./inputs.json --input-file ./config.json)', input_functions_1.castInputFile, [])
        .option('-q, --query <key=value>', 'Set query parameters as key=value pairs (e.g. --query foo=bar --query baz=qux)', input_functions_1.castInput, [])
        .option('--headers <key=value>', 'Set custom headers as key=value pairs (e.g. --headers X-API-Key=12345 --headers User-Agent=MyApp)', input_functions_1.castInput, [])
        .option('-n, --dry-run', 'Show the flow requestURL and payload that would be sent without making the actual request', false)
        .option('--show-secrets', 'Show actual authentication tokens in dry-run output (otherwise secrets are masked)', false)
        .option('-r, --result', 'Show only the response body', false)
        .addHelpText('after', '\nExamples:\n' +
        '  postman flows trigger 12345-67890-abcdef\n' +
        '  postman flows trigger 12345-67890-abcdef -i name=John -i age=30\n' +
        '  postman flows trigger 12345-67890-abcdef --input-file ./inputs.json\n' +
        '  postman flows trigger 12345-67890-abcdef -f ./inputs.json -i override=value\n' +
        '  postman flows trigger 12345-67890-abcdef --headers X-API-Key=12345 --headers User-Agent=MyApp\n' +
        '  postman flows trigger 12345-67890-abcdef --query version=v2 -q debug=true\n' +
        '  postman flows trigger 12345-67890-abcdef --result\n' +
        '  postman flows trigger 12345-67890-abcdef --dry-run\n' +
        '  postman flows trigger 12345-67890-abcdef --dry-run --show-secrets\n')
        .action((flowId, options) => {
        return (0, handler_1.validateAndRunCommand)({ ...options, flow: flowId }, flows_trigger_1.default);
    }));
    (0, handler_1.addCommonOptions)(flows
        .command('deploy <flowId>')
        .description('Deploy a flow (required to trigger it)')
        .usage('<flowId> [options]')
        .requiredOption('-p, --path <path>', 'Specify the URL path for the trigger')
        .option('-t, --timeout <timeout>', 'Specify the HTTP session timeout (5000ms to 60000ms)', '10000ms')
        .option('-a, --auth', 'Enable authentication for the trigger', false)
        .addHelpText('after', '\nExamples:\n' +
        '  postman flows deploy 12345-67890-abcdef --path /my-trigger\n' +
        '  postman flows deploy 12345-67890-abcdef --path /my-trigger --auth\n' +
        '  postman flows deploy 12345-67890-abcdef --path /my-trigger --timeout 5000ms\n')
        .action((flowId, options) => {
        return (0, handler_1.validateAndRunCommand)({ ...options, flow: flowId }, flows_deploy_1.default);
    }));
    (0, handler_1.addCommonOptions)(flows
        .command('run <path>')
        .description('Run a single flow from a file (e.g. ./path/to/flow.json)')
        .usage('<path> [options]')
        .option('-i, --input <key=value>', 'Set flow inputs as key=value pairs (e.g. --input foo=bar --input baz=qux)', input_functions_1.castInput, [])
        .option('-f, --input-file <path.json>', 'Set flow inputs from JSON files (e.g. --input-file ./inputs.json --input-file ./config.json)', input_functions_1.castInputFile, [])
        .option('-s, --scenario <name>', 'Use a pre-built scenario as flow inputs (by scenario name). ' +
        'Can be combined with --input and --input-file to override values.')
        .option('-e, --environment <path>', 'Specify a path to a Postman Environment file (JSON or YAML)')
        .option('--working-dir <path>', 'Specify the path to the working directory')
        .option('--verbose', 'Show detailed information of flow run and each request (method, URL, assertions)')
        .option('-x, --suppress-exit-code', 'Specify whether or not to override the default exit code for the current run')
        .addHelpText('after', '\nExample:\n' +
        '  postman flows run ./path/to/flow.json\n' +
        '  postman flows run ./path/to/flow.json -i name=John -i age=30\n' +
        '  postman flows run ./path/to/flow.json --input-file ./inputs.json\n' +
        '  postman flows run ./path/to/flow.json --scenario production --verbose\n')
        .action(flows_run_1.runFlowsCommand));
    (0, handler_1.addCommonOptions)(flows
        .command('update <flowId>')
        .description('Update settings for a deployed flow')
        .usage('<flowId> [options]')
        .option('-t, --trigger <on|off>', 'Enable or disable the flow trigger', (value) => {
        if (value !== 'on' && value !== 'off') {
            throw new Error('Invalid trigger value. Must be "on" or "off"');
        }
        return value;
    })
        .option('-a, --auth <on|off>', 'Enable or disable authentication for the flow trigger', (value) => {
        if (value !== 'on' && value !== 'off') {
            throw new Error('Invalid auth value. Must be "on" or "off"');
        }
        return value;
    })
        .addHelpText('after', '\nExamples:\n' +
        '  postman flows update 12345-67890-abcdef --trigger on\n' +
        '  postman flows update 12345-67890-abcdef --trigger off\n' +
        '  postman flows update 12345-67890-abcdef --auth on\n' +
        '  postman flows update 12345-67890-abcdef --auth off\n')
        .action((flowId, options) => {
        return (0, handler_1.validateAndRunCommand)({ ...options, flow: flowId }, flows_update_1.default);
    }));
    (0, handler_1.addCommonOptions)(flows
        .command('list-runs')
        .description('List run history for a deployed flow')
        .usage('[options]')
        .requiredOption('-w, --workspace <workspaceId>', 'Specify the workspace ID')
        .option('-f, --flow <flowId>', 'Filter sessions by flow ID')
        .option('-r, --range <range>', 'Time range to list runs (e.g. 30m, 2h, 3d)', '1h')
        .addHelpText('after', '\nExamples:\n' +
        '  postman flows list-runs --workspace 12345-67890-abcdef\n' +
        '  postman flows list-runs --workspace 12345-67890-abcdef --flow <flowId>\n' +
        '  postman flows list-runs --workspace 12345-67890-abcdef --range 30m\n' +
        '  postman flows list-runs --workspace 12345-67890-abcdef --range 3d\n')
        .action((options) => {
        const range = (0, flows_list_runs_1.parseTimeRange)(options.range || '1h');
        return (0, handler_1.validateAndRunCommand)({ ...options, range }, flows_list_runs_1.default);
    }));
    (0, handler_1.addCommonOptions)(flows
        .command('get-run')
        .description('Analyze a specific flow run')
        .usage('[options]')
        .requiredOption('-r, --run-id <runId>', 'Specify the run ID to analyze')
        .option('-l, --logs', 'Show detailed event log', false)
        .option('--filter <blockId>', 'Filter event log by block ID prefix (can be specified multiple times)', (value, previous) => { return [...(previous || []), value]; }, [])
        .addHelpText('after', '\nExamples:\n' +
        '  postman flows get-run --run-id session-abc123\n' +
        '  postman flows get-run --run-id session-abc123 --logs\n' +
        '  postman flows get-run --run-id session-abc123 --logs --filter blockId1\n')
        .action((options) => {
        return (0, handler_1.validateAndRunCommand)({ ...options, run: options.runId }, flows_analyze_runs_1.default);
    }));
}
//# sourceMappingURL=flows-cmds.js.map