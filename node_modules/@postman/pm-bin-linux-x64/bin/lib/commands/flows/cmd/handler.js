"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.addCommonOptions = addCommonOptions;
exports.validateCommand = validateCommand;
exports.validateAndRunCommand = validateAndRunCommand;
const zod_1 = require("zod");
const either_1 = require("../types/either");
const print_1 = __importDefault(require("../../../framework/print"));
function addCommonOptions(command) {
    return command
        .option('--verbose', 'Verbose output')
        .option('--debug', 'Debug output')
        .option('--json', 'JSON output');
}
function validateCommand(input, schema) {
    try {
        const data = schema.parse(input);
        return (0, either_1.right)(data);
    }
    catch (error) {
        let msg = 'Unknown validation error';
        if (error instanceof zod_1.z.ZodError) {
            const errorMessages = error.issues.map((err) => {
                return `${err.path.join('.')}: ${err.message}`;
            }).join(', ');
            msg = `Invalid command parameters: ${errorMessages}`;
        }
        else if (error instanceof Error) {
            msg = error.message;
        }
        return (0, either_1.left)(new Error(msg));
    }
}
async function validateAndRunCommand(c, h) {
    const validation = h.validateCommand(c);
    if ((0, either_1.isRight)(validation)) {
        await h.runCommand(validation.right);
    }
    else {
        print_1.default.lf(`Error: ${validation.left.message}`);
        process.exit(1);
    }
}
//# sourceMappingURL=handler.js.map