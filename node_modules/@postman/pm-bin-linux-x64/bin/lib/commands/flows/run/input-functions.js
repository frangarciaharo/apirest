"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.castInput = castInput;
exports.castInputFile = castInputFile;
exports.processInputFiles = processInputFiles;
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
const lodash_1 = __importDefault(require("lodash"));
const cmd_util_1 = __importDefault(require("../../cmd-util"));
function castInput(val, memo = []) {
    const equalIndex = val.indexOf('=');
    if (equalIndex === -1) {
        throw new Error(`Invalid --input format: "${val}". Expected key=value format.`);
    }
    if (equalIndex === 0) {
        throw new Error(`Invalid --input format: "${val}". Key cannot be empty.`);
    }
    return cmd_util_1.default.cast.memoizeKeyVal(val, memo);
}
function castInputFile(val, memo = []) {
    memo.push(val);
    return memo;
}
function processInputFiles(inputFiles) {
    const inputs = {};
    for (const filePath of inputFiles) {
        const jsonPath = path_1.default.resolve(filePath);
        if (!fs_1.default.existsSync(jsonPath)) {
            throw new Error(`--input-file JSON file not found: ${filePath}`);
        }
        let data;
        try {
            data = JSON.parse(fs_1.default.readFileSync(jsonPath, 'utf8'));
        }
        catch (e) {
            throw new Error(`Invalid --input-file JSON file "${filePath}": ${e.message}`);
        }
        if (typeof data !== 'object' || data === null || Array.isArray(data)) {
            throw new Error(`Invalid --input-file JSON file "${filePath}": must be a flat object`);
        }
        for (const [key, value] of Object.entries(data)) {
            const normalized = lodash_1.default.isNil(value) ? undefined : String(value);
            inputs[key] = normalized;
        }
    }
    return inputs;
}
//# sourceMappingURL=input-functions.js.map