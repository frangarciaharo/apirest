"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readEnvironmentFile = readEnvironmentFile;
exports.readEnvironmentFiles = readEnvironmentFiles;
const promises_1 = __importDefault(require("fs/promises"));
const liquid_json_1 = __importDefault(require("liquid-json"));
const file_utils_1 = require("../file-utils");
async function readEnvironmentFile(filePath) {
    const content = await promises_1.default.readFile(filePath, 'utf8');
    if ((0, file_utils_1.isYamlFile)(filePath)) {
        const yaml = require('js-yaml');
        const parsed = yaml.load(content);
        if (!parsed || typeof parsed !== 'object') {
            throw new Error(`Failed to parse YAML environment file: ${filePath}`);
        }
        return parsed;
    }
    return liquid_json_1.default.parse(content);
}
function readEnvironmentFiles(directory) {
    return (0, file_utils_1.scanDirectoryForFiles)(directory, file_utils_1.JSON_AND_YAML_EXTENSIONS);
}
//# sourceMappingURL=environment.js.map