const colors = require('colors/safe'), { SEVERITY_COLOR_MAP } = require('../constants');
function formatRange(result) {
    return `${result.range.start.line + 1}`;
}
function getFormattedSeverity(severity) {
    return colors[SEVERITY_COLOR_MAP[severity]](severity);
}
module.exports = {
    formatRange,
    getFormattedSeverity
};
//# sourceMappingURL=util.js.map