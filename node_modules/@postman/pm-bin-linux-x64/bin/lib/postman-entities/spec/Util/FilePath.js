const path = require('path'), os = require('os');
module.exports = {
    resolveRelativePath: (filePath) => {
        return path.relative(process.cwd(), path.resolve(path.normalize(filePath)));
    },
    convertToPosixPath: (filePath) => {
        if (os.platform() === 'win32') {
            return filePath.replaceAll(path.win32.sep.toString(), path.posix.sep.toString());
        }
        return filePath;
    }
};
//# sourceMappingURL=FilePath.js.map