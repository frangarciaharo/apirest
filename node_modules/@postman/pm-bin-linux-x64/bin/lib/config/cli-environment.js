const betaConfig = require('./cli-environment/beta'), stageConfig = require('./cli-environment/stage'), defaultConfig = require('./cli-environment/default'), CLI_ENVIRONMENT_CONFIGS = {
    beta: betaConfig,
    stage: stageConfig,
    prod: defaultConfig,
    dev: betaConfig,
    test: betaConfig
}, VALID_CHANNELS = Object.keys(CLI_ENVIRONMENT_CONFIGS);
let _cachedConfig = null;
function getCliEnvironmentConfig() {
    if (_cachedConfig) {
        return _cachedConfig;
    }
    const channel = process.env.POSTMAN_CHANNEL, env = VALID_CHANNELS.includes(channel) ? channel : 'prod';
    _cachedConfig = CLI_ENVIRONMENT_CONFIGS[env];
    return _cachedConfig;
}
function load(callback) {
    return getCliEnvironmentConfig().load(callback);
}
function getSettings() {
    return getCliEnvironmentConfig().getSettings();
}
module.exports = {
    load,
    getSettings
};
//# sourceMappingURL=cli-environment.js.map