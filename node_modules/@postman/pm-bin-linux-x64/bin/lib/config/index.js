var _ = require('lodash'), async = require('async'), env = require('./process-env'), cliEnvironment = require('./cli-environment'), rcfile = require('./rc-file'), validateAccessToken = require('./validateAccessToken');
module.exports.get = (cliOptions, options, callback) => {
    var { command } = options;
    async.parallel([
        cliEnvironment.load,
        (cb) => {
            if (options.ignoreRcFile) {
                return cb(null, {});
            }
            return rcfile.load(['home', 'cwd'], cb);
        },
        !options.ignoreProcessEnvironment ? env.load : (cb) => {
            return cb(null, {});
        }
    ], (err, options) => {
        if (err) {
            return callback(err);
        }
        let [, rcOptions] = options, aliasDetails;
        options = _.map(options, (obj) => {
            return obj && obj[command] ? obj[command] : {};
        });
        options = _.assignWith({}, ...options, cliOptions, (dest, src) => {
            return (src === null) ? dest : undefined;
        });
        if (!options.postmanApiKey) {
            rcOptions.login &&
                ([aliasDetails] = _.filter(rcOptions.login._profiles, ['alias', options.alias]));
            if (aliasDetails) {
                options.postmanApiKey = aliasDetails;
                options.accessToken = aliasDetails.accessToken;
                options.userId = aliasDetails.userId;
            }
        }
        delete options.alias;
        if (options.accessToken) {
            return validateAccessToken(options.accessToken, (err) => {
                if (err) {
                    return callback(err);
                }
                return callback(null, options);
            });
        }
        return callback(null, options);
    });
};
//# sourceMappingURL=index.js.map