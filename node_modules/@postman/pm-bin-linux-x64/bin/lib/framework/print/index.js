var format = require('util').format, cliUtils = require('../../reporters/cli/cli-utils'), SPC = ' ', BS = '\b', LF = '\n', WAIT_FRAMES = (/^win/).test(process.platform) ?
    ['\u2015', '\\', '|', '/'] :
    ['⠄', '⠆', '⠇', '⠋', '⠙', '⠸', '⠰', '⠠', '⠰', '⠸', '⠙', '⠋', '⠇', '⠆'], WAIT_FRAMES_SIZE = WAIT_FRAMES.length - 1, WAIT_FRAMERATE = 100, print, notSilent = true;
print = function () {
    return print.print.apply(this, arguments);
};
print.print = function () {
    print.waiting && print.unwait();
    print._buffer && print.unbuffer();
    notSilent && process.stdout.write(format.apply(this, arguments));
    return print;
};
print.lf = function () {
    print.waiting && print.unwait();
    print._buffer && print.unbuffer();
    notSilent && process.stdout.write(format.apply(this, arguments) + LF);
    return print;
};
print._waitPosition = 0;
print.wait = function (color) {
    print.unwait();
    if (cliUtils.noTTY()) {
        return print;
    }
    notSilent && process.stdout.write(SPC);
    print.waiting = setInterval(function () {
        notSilent && process.stdout.write(BS +
            (color ? color(WAIT_FRAMES[print._waitPosition]) : WAIT_FRAMES[print._waitPosition]));
        (++print._waitPosition > WAIT_FRAMES_SIZE) && (print._waitPosition = 0);
    }, WAIT_FRAMERATE);
    return print;
};
print.unwait = function (clearSpinner = false) {
    if (print.waiting) {
        print.waiting = clearInterval(print.waiting);
        print._waitPosition = 0;
        notSilent && process.stdout.write(clearSpinner ? '\b ' : '\b');
    }
    return print;
};
print._buffer = undefined;
print.buffer = function (startText, endText) {
    print.waiting && print.unwait();
    (print._buffer === undefined) && notSilent && process.stdout.write(startText);
    print._buffer = endText;
    print._buferring && (print._buferring = clearTimeout(print._buferring));
    print._buferring = setTimeout(print.unbuffer, 500);
    notSilent && process.stdout.write(format.apply(this, Array.prototype.splice.call(arguments, 2)));
    return print;
};
print.nobuffer = function () {
    print.unwait();
    notSilent && process.stdout.write(format.apply(this, arguments));
    return print;
};
print.unbuffer = function () {
    print._buferring && (print._buferring = clearTimeout(print._buferring));
    if (print._buffer) {
        notSilent && process.stdout.write(print._buffer);
        print._buffer = undefined;
    }
    return print;
};
print.setup = function (silent = false) {
    notSilent = !silent;
};
module.exports = print;
//# sourceMappingURL=index.js.map