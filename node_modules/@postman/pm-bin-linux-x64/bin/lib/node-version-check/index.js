var semver = require('semver'), colors = require('colors/safe'), pkg = require('../../package.json'), console = require('../framework/console'), requiredNodeVersion = pkg && pkg.engines && pkg.engines.node, currentNodeVersion = process && process.version;
if (requiredNodeVersion && currentNodeVersion) {
    if (!semver.satisfies(currentNodeVersion, requiredNodeVersion)) {
        console.error([colors.red('postman:'), 'required node version', requiredNodeVersion].join(' '));
        process.exit(1);
    }
}
//# sourceMappingURL=index.js.map