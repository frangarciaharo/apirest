"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadV3AsExtensibleCollection = loadV3AsExtensibleCollection;
const v3_import_1 = require("@postman/v3.import");
const v3_1 = require("@postman/runtime.models/v3");
const transforms_1 = require("@postman/runtime.models/transforms");
const sdk_utils_1 = require("../commands/sdk/sdk-utils");
const crypto_1 = __importDefault(require("crypto"));
const path_1 = __importDefault(require("path"));
const console_1 = __importDefault(require("../framework/console"));
function generateObjectId() {
    return crypto_1.default.randomBytes(12).toString('hex');
}
function ensureIds(node) {
    if (!node) {
        return;
    }
    if (!node.id) {
        node.id = generateObjectId();
    }
    if (Array.isArray(node.children)) {
        node.children.forEach((child) => { return ensureIds(child); });
    }
}
async function loadV3AsExtensibleCollection(filePath) {
    const v3Path = (0, sdk_utils_1.detectV3Collection)(filePath);
    if (!v3Path) {
        return null;
    }
    try {
        const normalizedV3Path = path_1.default.resolve(v3Path);
        const v3Collection = await (0, v3_import_1.importCollection)(normalizedV3Path);
        const parsed = v3_1.Collection.parse(v3Collection);
        const EC = (0, transforms_1.transform)(v3_1.Collection, transforms_1.FormatVersion.Extensible, parsed);
        ensureIds(EC);
        return EC;
    }
    catch (error) {
        console_1.default.error(`Failed to load V3 collection: ${error.message}`);
        return null;
    }
}
//# sourceMappingURL=v3-loader.js.map