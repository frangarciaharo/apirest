const USER_FACING_MESSAGES = {
    ERR_INVALID_ENTRY_POINTS: 'Unable to find request or folder. ' +
        'Please ensure the specified request or folder exists in the collection.'
};
module.exports = function (err) {
    if (!err || !err.code || !USER_FACING_MESSAGES[err.code]) {
        return err;
    }
    return new Error(USER_FACING_MESSAGES[err.code]);
};
//# sourceMappingURL=get-user-facing-error.js.map