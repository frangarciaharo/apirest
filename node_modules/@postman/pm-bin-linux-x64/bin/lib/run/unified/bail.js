"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createBailController = createBailController;
const _ = require("lodash");
function createBailController(options) {
    const bail = options.bail;
    const bailObj = (typeof bail === 'object' && bail !== null) ? bail : undefined;
    const stopOnFailure = bail === true || Boolean(bailObj?.failure);
    const bailFolders = Boolean(bailObj?.folder);
    if (!stopOnFailure && !bailFolders) {
        return {
            beforeEmit: () => { return false; },
            afterEmit: () => { return false; }
        };
    }
    const failedFolders = new Set();
    let currentFolders = [];
    return {
        beforeEmit(event) {
            if (event.type !== 'collection:item') {
                return false;
            }
            currentFolders = _.get(event, 'payload.folders', []);
            return bailFolders &&
                currentFolders.some((f) => { return failedFolders.has(f); });
        },
        afterEmit(event) {
            if (event.type !== 'scripting:assertion') {
                return false;
            }
            const hasFailed = _.some(_.get(event, 'payload.assertions', []), (a) => { return !a.passed && !a.skipped; });
            if (!hasFailed) {
                return false;
            }
            if (bailFolders) {
                currentFolders.forEach((f) => { return failedFolders.add(f); });
            }
            return stopOnFailure;
        }
    };
}
//# sourceMappingURL=bail.js.map